# C Football Game
C++ Terminal Football

!todo
* Embrace Smart Pointers (instead of new/delete)
* \#include <random>, use std::mt19937 instead of std::srand and rand()
* Use constant reference (ex: std::string& new_name) instead of passing in raw value which copies the string
* Create a centralized League class that College and Pro_League derive from
```
